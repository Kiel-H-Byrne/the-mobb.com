import React from 'react'
import PropTypes from 'prop-types'

const ClosestCard = {activeListing} => {
  return (
    <div>
      
    </div>
  )
}

ClosestCard.propTypes = {
  activeListing: PropTypes.object
}

export default ClosestCard
